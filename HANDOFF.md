# 앱과 도구 — 구조 · 조심할 것

**이 파일에는 진도·차시 상태를 적지 않습니다.** 그건 `docs/course-map.md` 한 곳에만 있습니다.
(두 곳에 적었다가 이 파일이 옛 상태로 남아 실제로 자료를 잘못 만든 적이 있습니다. 2026-09-05 정리)

| 볼 것 | 무엇이 |
|---|---|
| `CLAUDE.md` | 수업자료를 만들 때 지킬 규칙 (대화마다 자동으로 읽힙니다) |
| **`docs/course-map.md`** | **진도 · 평가 · 다룰 것/뺄 것 · 차시별 기록 · 앱에 올린 것** — 자료를 만들기 전 반드시 |
| `HANDOFF.md` (이 파일) | 앱 코드 구조 · 도구 쓰는 법 · 조심할 것 |
| `tools/deck-spec.md` | 슬라이드 배치 종류와 옵션 |

이 넷 말고는 읽지 않습니다. `docs/archive/` 는 옛 문서라 지금 상태와 어긋납니다.

---

## 프로젝트

신동고등학교 **인공지능 기초 · 진로** 수업용 웹앱입니다. 사용자는 정보 교사이고, 수업 도구를 직접 만들어 씁니다.

| | |
|---|---|
| 레포 | `JunRepos/ai-career` |
| 로컬 | `C:\Users\PC\Desktop\github\ai-career` |
| 배포 | https://junrepos.github.io/ai-career/ (GitHub Pages, `main` 브랜치) |
| 스택 | 순수 vanilla JS (빌드 없음) + Firebase RTDB/Storage |

`informatics` 레포(정보 교과 앱)에서 갈라져 나왔습니다. **정보 교과 코드(OJ·노트북·미션·퀴즈·AI코딩·기계학습·수행평가)는 하나도 지우지 않고 메뉴에서만 빼둔 상태입니다. 지우지 마세요.**
`js/games/` 의 `flappybird` · `typehunter` · `lockerdrop` 도 같은 이유로 남아 있습니다 (`GAMES` 목록에는 없음).

## 반 구성

**`ai-2B`(수·목·금) · `ai-2D`(월·화·금) 로만 부릅니다.** 진도표 엑셀의 '2반/3반' 과 어긋납니다 (2반 = ai-2D, 3반 = ai-2B).

| 과목 | 반 id | 인원 |
|---|---|---|
| 인공지능 기초 | `ai-2B`(수3교시) · `ai-2D`(월2교시) | 37 · 16 |
| 진로와 직업 | `career-2-1` ~ `career-2-6` | 20~23명씩 |

- 학생 로그인: **학번 / 학번**, 첫 로그인 시 비밀번호 변경 강제(`isFirstLogin`)
- 확인용 계정: 모든 반에 **`test` / `test`** (의도된 것 — 지우지 마세요)
- 선생님 비밀번호는 informatics 앱과 **같은 것**

## ⚠ Firebase 는 informatics 와 공유합니다

같은 프로젝트(`sindong-informatics`)를 쓰고 데이터는 반 id 로만 갈립니다.
**보안 규칙 파일은 프로젝트 전체를 덮어씁니다.** 규칙을 고칠 일이 생기면 **두 레포의 `database.rules.json`·`storage.rules` 를 같은 내용으로 맞춘 뒤** 배포하세요.

```bash
firebase deploy --only storage --project sindong-informatics
```

- 규칙 반영에 **1분**쯤 걸립니다. 배포 직후 권한 오류가 나면 규칙이 틀린 게 아니니 기다렸다 다시 확인하세요.
- 규칙 비교 전에 **로컬 informatics 가 최신인지 `git fetch` 로 먼저 확인**하세요. (뒤처진 로컬을 보고 "두 레포가 다르다"고 오진한 적 있음)

---

## 앱 구조

**진입** 과목 선택 → 도트 전환 → 반 선택 → 로그인
**디자인** Padlet 톤(다크 기본), 반·단원마다 다른 9×9 도트 아이콘

- **학생** — 나의 포트폴리오 · 홈 · 공지 · 단원(인공지능만) · 과제 제출 · 학습지 · 수업자료
- **선생님** — 포트폴리오(세특용 텍스트 복사) · 공지 · 수업자료 · 출결 · 학생관리 · 단원 구성 · 학습지

### 수업자료 (슬라이드)

- 차시별로 **여러 세트 보관** → 그중 **'이번 시간에 열기'** 한 자료가 학생에게 노출
  - `slides/{반}/decks/{자료id}` · `slides/{반}/live = {on, page, deckId}`
- **여러 반에 한 번에 업로드** (기본: 같은 과목 전체 체크). 여러 반에 올려도 **자료 id 는 하나로 통일**
- **같이 보기** — 선생님이 넘기면 학생 화면이 따라옴
- **발표 모드** (전체화면, ←→·PageUp/Down·B 검은화면·**N 다음 장 미리보기**)
- **🎤 발표자 보기** — 창 두 개. 프로젝터엔 발표 모드, 노트북엔 다음 장 + **내 대본 메모** + 경과 시간
  - 대본 메모는 `slides/{반}/tcNotes/{자료id}/{쪽}` — **학생에게 안 보임** (학생 메모와 별개)
  - ⚠ 브라우저 **팝업 차단 허용** 필요
- 학생은 장마다 메모 가능 (`slides/{반}/notes/{자료id}/{학번}/{쪽}` — 자료별로 분리)
  - 입력칸에 **그림·파일을 끌어다 놓는 것은 막아 뒀습니다** (2026-08-26). 안 막으면 브라우저가 그림 주소를 글자로 넣습니다 — 실제로 한 학생이 13장에 걸쳐 주소만 남긴 적이 있습니다. `js/app.js` 에 문서 전체로 한 번, **발표자 보기 창은 별개 문서라** `js/views/slides.js` 에 따로

> ⚠ **같은 자료 id 라도 반마다 장수가 다를 수 있습니다.** 선생님이 앱에서 **실습 게임 장을 끼워 넣으면**
> 그 반의 덱에만 한 장이 늘어납니다(덱 `images[]` 안의 `{type:'game', gameId}`).
> `--list` 의 장수가 반마다 다르면 대개 이것입니다. 한쪽 반에만 다시 올려도 갈립니다.

### 학습지 (예전 이름 '활동지' — 용어 통일함)

- 기본 학습지는 코드(`js/aiactivity-data.js`)에, **선생님이 만든 것은 반 DB**(`aiactivity/submissions/{반}/customActivities`)에
- 편집기에서 문항 유형 **글 / 안내 / 체크박스 / 표** 를 만들 수 있음
  - 표 **자동 채우기**: 앞선 체크박스에서 고른 보기가 표 첫 칸에 들어감(`fillFrom`)
- 기본 학습지는 **[복제해서 수정]** / **[목록에서 빼기]**(되돌리기 가능), 만든 학습지는 **[수정] [삭제]**
- **[학생에게 보내기]** 를 눌러야 학생 홈에 뜸

### 단원 구성 (인공지능만)

- `수업 자료` / `학습 활동` 두 칸
- 넣을 수 있는 유형: **실습 게임 · 학습지 · 수업자료 · 파일 · 링크 · 글**
- 단원에 건 수업자료는 **수업 때 쓴 필기가 그대로** 남은 채 열림 (혼자 보기 — 선생님을 안 따라감)
- 단원에 건 학습지는 **'학생에게 보내기' 안 눌러도** 열림

### 실습 게임

목록은 `js/games/games.js` 의 **GAMES** 한 곳에서 관리합니다. 게임을 더 만들면
파일 하나 + 여기 한 줄이면 끝입니다. (`views/unit.js` 의 UC_GAMES 가 이걸 그대로 씁니다)

| id | 게임 | 내용 | 판 검산 |
|---|---|---|---|
| `plant-water` | 🌿 식물 물 주기 | 4×4 · 30초 · 끝나면 '방금 쓴 지능' 되짚기 | |
| `maze` | 🧭 미로 탐사 | 갈림길이 곧 상태 · 너비/깊이 우선으로 직접 탐사 | `verify/maze.mjs` (판은 `maze-gen.mjs` 로 뽑음) |
| `city-cost` | 🏙 도시 배달 | 길마다 시간이 다른 도시 · 균일 비용 탐색을 스스로 찾아냄 | `verify/citycost.py` |
| `city-astar` | 🧭 도시 배달 — 지도를 보고 | 도시마다 **직선거리 h** 가 적힌 지도 · `f = g + h` 가 작은 곳부터 | `verify/cityastar.py` (판 설계 `cityastar-design.py`) |
| `puzzle-astar` | 🧠 8퍼즐 A\* 탐색 | **교과서 37쪽 활동3** · 학생이 **g·h·f 를 직접 입력**해야 다음으로 | `verify/lesson6.py` |
| `puzzle-8` | 🧩 8퍼즐 맞추기 | **학습지 앞면과 같은 판**(`_13/824/765` → `123/8_4/765`) · 맞추면 깊이 2 탐색 트리 | |

브라우저로 `tools/game-preview.html` 을 열면 앱 없이 게임만 따로 확인됩니다
(`?game=maze&start=1` 처럼 열면 시작한 화면부터).

> ⚠ **8퍼즐은 2026-08-31 에 다시 만들었습니다.** 예전에는 타일을 `transform` 으로 옮기고
> `document` 전체에서 판을 찾아 값만 갈아끼웠는데, **내부 상태와 화면이 어긋나** 타일이 남을
> 뛰어넘고 '수'가 0에 멈추는 일이 실제로 수업 중에 났습니다. 지금은 상태가 바뀔 때마다
> `#p8-app` 안을 통째로 다시 그립니다. **게임 화면에서 `document` 전체를 훑는 선택자를 쓰지 마세요.**

- 슬라이드 중간에 끼우기 — 수업자료 조작판의 `＋ 🌿 …` `＋ 🧩 …` 버튼 (지금 장 **다음**에 들어감)
- 점수는 `slides/{반}/scores/{게임id}/{학번}` 에 **최고 기록만**. 저장 함수가 '클수록 좋음' 이라
  적을수록 좋은 게임은 빼서 넣습니다 — 8퍼즐은 `1000 − 수`

> 「숨바꼭질」은 2026-08-27 에 **폐기**했습니다. 코드·CSS·DB 항목을 전부 걷어냈습니다.
> 되살릴 일이 있으면 `.backup/hideseek-2026-08-27/` 에 있습니다.

---

## 코드에서 반드시 지킬 것 (다 대가를 치르고 배운 것)

1. **Firebase 호출은 막히면 실패가 아니라 영원히 대기합니다.** 초기 로드는 `app.js` 의 `withTimeout()`, 업로드는 `firebase.js` 의 멈춤 감시(30초)로 막아뒀습니다. **새 초기 로드·업로드를 추가하면 반드시 같은 방식으로 감싸세요.**
2. **CSS 색 토큰은 `:root` 에도 정의하세요.** `[data-theme="dark"]` 안에만 넣으면 라이트 모드에서 값이 비어 요소가 통째로 사라집니다.
3. **전역 `button{display:inline-flex}` 가 있습니다.** 카드형 버튼은 `display:block` 을 따로 주세요.
4. **클릭 핸들러는 `data-action` 없는 클릭을 걸러냅니다.** `id` 만 달면 눌러도 아무 일이 안 일어납니다.
5. **CSS 는 `css/styles.css` 맨 끝 오버라이드 레이어에만 추가합니다.**
6. **게임·타이머가 도는 화면에서 `render()` 를 반복 호출하지 마세요.** 0.1초마다 다시 그리면 누르는 순간 버튼이 사라져 **클릭이 아예 안 먹습니다.** (물주기가 이 버그였음 — 지금은 `_pwPaint()` 로 값만 갈아끼움)
7. **여러 반을 다루는 기능은 '모든 대상 반'을 처리했는지 확인하세요.** 지금 보고 있는 반만 처리해서 다른 반 자료가 학생 화면에서 조용히 사라진 적 있습니다.
8. **삭제·설정은 반별입니다.** 학습지를 지워도 다른 반엔 그대로 남습니다.
9. **배포 후 `?v=` 를 갱신하세요.** `sh bump-version.sh` 를 돌리면 `index.html` 의 js/css 주소 버전이 바뀌어 학생 브라우저가 새 파일을 받습니다. 안 하면 옛 코드가 계속 돕니다.
10. **작업 후 반드시 배포까지 하고 브라우저로 확인**하세요. push 만 하고 넘기지 마세요.
11. **테스트로 만든 데이터는 반드시 지우세요.** 실제 수업 DB입니다. (지우기 전 학생 답안이 있는지 먼저 확인)
12. **게임은 `pw*`(물 주기 전용) 함수를 직접 부르지 말고 `games.js` 의 갈래 함수를 쓰세요.**
    `gameView(id)` · `gameLoadRank(id)` · `gameLeaveAll()` · `gameTeacherBoard(id)`.
    `gameDef()` 가 모르는 id 를 물 주기로 떨어뜨리기 때문에, 잘못 불러도 화면이 안 비고
    **엉뚱한 게임이 조용히 뜹니다.**

---

## 수업자료를 만드는 길

**교과서·노트 → `verify/lessonN.py` 로 검산 → `verify/lessonN-deck.py` 가 덱 JSON → pptx → 선생님이 PowerPoint 로 손보고 → PNG → 앱에 올림**

```bash
python verify/lessonN.py                 # 알고리즘을 실제로 돌려 lessonN-facts.json 을 냅니다
python verify/lessonN-deck.py            # facts 를 읽어 tools/samples/lessonN-*.deck.json 을 만듭니다
node   tools/deck-build.mjs <덱.json>     # pptx
node   tools/deck-png.mjs <파일.pptx>     # 슬라이드1.PNG …
node   tools/deck-upload.mjs --list      # 반별로 어떤 자료가 있고 무엇이 열려 있는지
node   tools/deck-upload.mjs <PNG폴더> --title "6. 지능적 탐색" --classes ai-2B,ai-2D
```

`verify/layout-check.mjs` 는 **불러 쓰는 도구**입니다(직접 실행하면 아무 일도 안 합니다).
`diagram` 노드가 겹치는지 · 간선이 남의 노드를 스치는지 검사합니다. 손으로 좌표를 놓을 때
작은 `.mjs` 하나를 만들어 `import { check }` 로 씁니다 — 예는 `docs/archive/verify-2026-08/`.

- **숫자·순서는 손으로 옮겨 적지 않습니다.** 검산기가 낸 `facts` 를 생성기가 읽어 씁니다 (CLAUDE.md 14·15)
- **덱 JSON 을 직접 고치지 마세요.** 생성기를 고쳐 다시 뽑습니다
- 배치 종류와 옵션은 `tools/deck-spec.md`, 견본은 `tools/samples/layouts-catalog.deck.json`
- 한 장에 안 들어가면 만들 때 ⚠ 경고가 뜹니다
- 파일 이름은 `tools/samples/README.md` 의 **`lessonN` = 진도표 차시** 규칙을 따릅니다

| 어디에 | 무엇이 |
|---|---|
| 교과서 PDF | `D:/Google drive/신동고등학교/01_교과_인공지능/교과서 및 교사용 지도서/고_인공지능 기초(김현철)_교과서.pdf` |
| 차시 노트 | `D:/Google drive/Obsidian Vault/01_Projects/수업/인공지능 기초/` |
| 붙여넣은 그림 | `D:/Google drive/Obsidian Vault/04_Archive/99_media/` |
| 새 차시 템플릿 | `D:/Google drive/Obsidian Vault/98_Templates/T_인공지능 차시.md` |

> 📖 **교과서 쪽 = PDF 쪽 − 3** (PDF 31쪽 = 책 28쪽). `python + fitz` 로 읽고 **결과를 UTF-8 파일로
> 저장한 뒤 읽습니다** — 콘솔로 바로 찍으면 한글이 깨집니다(cp949).
> 이 교과서에는 **'상태 정보' 라는 말이 없습니다** — 그 자리에 **간선**을 씁니다.

> **`deck-upload.mjs` 는 앱을 안 거치고 바로 올립니다** (Firebase REST). 앱의 '새 자료 올리기'와
> 같은 자리·같은 모양으로 넣고, **두 반에 같은 자료 id** 를 씁니다. 한 반이 실패하면 그 반의
> 조각 파일을 지우고, 끝나면 DB 를 다시 읽어 확인합니다. `--dry` 로 미리 볼 수 있습니다.
> ⚠ **PNG 폴더 경로를 맨 앞에** 두세요. 플래그 뒤에 두면 값으로 먹혀 폴더를 못 찾습니다.
> **`이번 시간에 열기`(live)는 건드리지 않습니다** — 수업 때 선생님이 직접 누릅니다.

> ⚠ `deck-png.mjs` 는 **이미 떠 있는 PowerPoint 를 안 닫습니다** (2026-08-27 고침).
> 저장 안 한 편집이 있으면 아예 시작하지 않고 알려 줍니다.

### 슬라이드별로 고칠 것을 적어 두면 그대로 고칩니다

장마다 메모를 적어 두면 클로드가 읽어서 반영합니다. **고쳐 달라는 줄은 맨 앞에 `>>`** 를
붙입니다. 그래야 진짜 대본과 섞이지 않고 `--only` 로 그 줄만 뽑아 옵니다.

```
휴리스틱값 정의는 여기서 천천히.
>> 이 장 제목 너무 기니까 짧게 줄여줘
```

| 어디에 | 언제 편한가 | 읽는 법 |
|---|---|---|
| **앱** — 수업자료의 `🎤 N장에서 할 말` 칸 | 이미 올린 자료를 앱·폰으로 넘겨 보며 | `node tools/deck-notes.mjs --id <자료id> --class ai-2B --only` |
| **PowerPoint 발표자 노트** | pptx 를 열어 놓고 손볼 때 | `node tools/deck-notes.mjs --pptx "…/파일.pptx" --only` |

**⚠ 고치는 방법이 두 가지고, 어느 쪽이냐에 따라 결과가 다릅니다.**

- **메모만 적으셨으면** — 덱 생성기를 고쳐 다시 뽑습니다. 깔끔합니다.
- **PowerPoint 로 상자를 옮기는 등 손을 대셨으면** — 다시 뽑으면 그 편집이 날아갑니다.
  그때는 **고칠 장만 따로 만들어** `deck-upload.mjs --replace-page` 로 갈아 끼웁니다.

**앱 메모는 쪽 번호(0부터)로 저장됩니다.** 장을 빼거나 더하면 뒤쪽 메모가 밀립니다.

### 노트에 적는 슬라이드 지시 (선생님과의 약속)

**기본은 지시 없음** — 노트에 수업 내용만 쓰고 배치는 클로드가 정합니다.
원하는 그림이 있을 때만 그 자리에 한 줄:

```markdown
> [!slide] 여기는 표로 비교하면 좋겠어
```

PPT 를 보고 **말로 고쳐도** 됩니다 — 그때는 고쳐 뽑은 뒤 그 지시를 노트에도 한 줄로 적어 둡니다.

## 학습지 (앱)

1. HTML 이나 초안을 받습니다
2. **`node tools/worksheet-preview.mjs sheet.json`** 으로 미리보기를 만들어 보여드립니다
   - 앱의 진짜 학습지 CSS 를 긁어 쓰므로 실제 화면과 같습니다
   - **미리보기에서 문장을 직접 고칠 수 있습니다** → [고친 내용 복사] 로 돌려받습니다
3. 확정되면 **코드에 넣지 말고 반 DB 에** `saveCustomActivity(반, id, payload)` 로 올립니다
   - 그래야 앱에서 **수정·삭제**할 수 있습니다 · **두 반 모두 같은 id** · 고칠 때는 **같은 id 로 덮어씀**
4. **수업자료 슬라이드와 대조**합니다. 용어·단계 수가 어긋나면 학생이 헷갈립니다 (CLAUDE.md 18)

## 종이 학습지 (인쇄물)

- 원본은 **A4 인쇄용 HTML** 한 파일 (`tools/samples/lesson5-worksheet.html`)
  - `@page { size:A4 portrait; margin:11mm 14mm }` · 본문 폭 `182mm` · 판·트리는 **SVG 로 직접**
  - `html,body{background:#fff; color-scheme:light only}` 를 꼭 넣으세요. 없으면 **다크 모드에서 검게** 렌더됩니다
  - 흑백으로 뽑아도 읽히게 — 색은 옅은 회색·검정 위주로
- PDF 는 **헤드리스 크롬**으로 (LibreOffice 없이 됩니다). 경로는 **`file:///C:/…` 윈도 형식**이어야 합니다

```bash
"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu \
  --no-pdf-header-footer --print-to-pdf="out.pdf" "file:///C:/…/sheet.html"
```

- 뽑은 PDF 는 **눈으로 반드시 확인** (`python + fitz` 로 PNG). 트리가 넘쳐 잘린 적, 제목이 깨진 적 있습니다
- 슬라이드의 활동 장과 **겹치지 않게** — 종이로 옮겼으면 슬라이드에는 '학습지 받고 시작' 한 장 + 답 확인만

---

## 컴퓨터를 갈아엎기 전에 — 백업

```bash
powershell -ExecutionPolicy Bypass -File tools/backup-claude.ps1
```

`D:/Google drive/ai-career-backup/` 에 **깃허브에 없는 것만** 모읍니다. 여러 번 돌려도 됩니다(안 지웁니다).

| 담는 것 | 왜 |
|---|---|
| `~/.claude/projects` 등 | **클로드 코드 대화 기록·메모리.** 레포가 아니라 홈 폴더에 있습니다 |
| `.claude/launch.json` | 미리보기 서버 설정 — `.gitignore` 에 걸려 레포에 없음 |
| 손본 `*.pptx` | `tools/samples/*.pptx` 가 `.gitignore` 라 레포에 없음 |
| `RESTORE.md` | 되살리는 법 |

- **레포만 clone 하면 하네스는 다 살아납니다** — `CLAUDE.md`·`docs/`·`tools/`·`verify/` 가 전부 git 에 있습니다
- **대화 기록 되살리기는 보장이 안 됩니다.** 세션 폴더 이름이 **절대 경로를 인코딩**합니다
  (`C--Users-PC-Desktop-github-ai-career`). 레포를 **같은 경로**에 두고 `claude-home/` 을 `~/.claude/` 로
  되돌린 뒤 `claude --resume` 해야 하며, 사용자 이름이 다르면 폴더 이름을 손으로 바꿔야 합니다
- 옵시디언 볼트·교과서 PDF 는 원래 구글 드라이브에 있으니 백업 대상이 아닙니다

⚠ `tools/backup-claude.ps1` 은 **UTF-8 BOM 으로 저장**해야 합니다. PowerShell 5.1 은 BOM 이
없으면 `.ps1` 을 cp949 로 읽어 한글이 전부 깨집니다. (2026-08-28 실제로 겪음)

---

## 🔴 보류 중 — 보안

Firebase 규칙에 **인증 개념이 아예 없습니다.** 모든 경로가 `.read: true` + 반 id 정규식만 확인하는 `.write` 입니다.

- `auth/teacher` 가 **읽기·쓰기 모두 열려** 있어 학생이 콘솔에서 **선생님 비밀번호를 덮어쓸 수 있습니다**
- `students/{반}` 이 열려 있어 **전교생 학번·이름·비번해시**가 로그인 없이 읽힙니다

고치려면 로그인 방식 자체를 바꿔야 해서(지금은 클라이언트에서 해시 비교) 설계 결정이 필요합니다.
**선생님이 하자고 할 때 합니다.**

## 환경

- Windows. **PowerPoint(COM) · Python(python-pptx, pywin32, PIL, fitz) · Node** 설치돼 있음.
  슬라이드 생성기는 `pptxgenjs`(`npm install`). pptx 렌더는 LibreOffice 말고 **PowerPoint COM**
  (LibreOffice 는 이 환경에서 실행 안 됨)
- 콘솔은 cp949 — **한글 결과는 UTF-8 파일로 저장한 뒤 읽습니다**
- 로컬 확인은 `.claude/launch.json` 의 `ai-career`(http://localhost:5599) · 워크트리는 `ai-career-wt`(5601)
  - ⚠ 다른 대화가 5599 를 잡고 있으면 **그 폴더가 서빙**됩니다. 워크트리에서 확인할 때는 5601 을 쓰세요

## 말투

한국어로, **결론부터**. 확인 없이 단정하지 말고 **실제로 돌려보고** 알려주세요. 안 되면 안 된다고
솔직히 말해주세요. 모르는 걸 임의로 정하지 말고 **모르면 물어보세요.**
