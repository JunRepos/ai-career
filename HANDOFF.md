# 이어서 작업하기 — 인수인계

새 대화 시작할 때 아래 내용을 그대로 붙여넣으면 됩니다.

---

## 프로젝트

신동고등학교 **인공지능 기초 · 진로** 수업용 웹앱입니다. 제가 정보 교사이고, 수업 도구를 직접 만들어 씁니다.

| | |
|---|---|
| 레포 | `JunRepos/ai-career` |
| 로컬 | `C:\Users\PC\Desktop\github\ai-career` |
| 배포 | https://junrepos.github.io/ai-career/ (GitHub Pages, main 브랜치) |
| 스택 | 순수 vanilla JS (빌드 없음) + Firebase RTDB/Storage |

`informatics` 레포(정보 교과 앱)에서 갈라져 나왔습니다. **정보 교과 코드(OJ·노트북·미션·퀴즈·AI코딩·기계학습·수행평가)는 하나도 지우지 않고 메뉴에서만 빼둔 상태입니다. 지우지 마세요.** 나중에 쓸 수도 있습니다.

## 반 구성

| 과목 | 반 id |
|---|---|
| 인공지능 기초 | `ai-2B`(36명, 수3교시), `ai-2D`(15명, 월2교시) |
| 진로와 직업 | `career-2-1` ~ `career-2-6` (test 계정만 등록됨) |

- 학생 로그인: **학번 / 학번**, 첫 로그인 시 비밀번호 변경 강제(`isFirstLogin`)
- 확인용 계정: 모든 반에 **`test` / `test`** (비번 변경 강제 없음)
- 선생님 비밀번호는 informatics 앱과 **같은 것**을 씁니다

## ⚠ Firebase는 informatics와 공유합니다

같은 프로젝트(`sindong-informatics`)를 쓰고 데이터는 반 id로만 갈립니다.

**보안 규칙 파일은 프로젝트 전체를 덮어씁니다.** 규칙을 고칠 일이 생기면 **두 레포의 `database.rules.json`·`storage.rules`를 같은 내용으로 맞춘 뒤** 배포해야 합니다. 한쪽만 배포하면 다른 쪽 반이 전부 저장 불가가 됩니다.

```bash
firebase deploy --only database,storage --project sindong-informatics
```

**규칙은 반영에 1분쯤 걸립니다.** 배포 직후 테스트하면 `storage/unauthorized`가 나옵니다 — 규칙이 틀린 게 아니니 잠시 기다렸다 다시 확인하세요. (제가 이걸로 한 번 오진했습니다)

## 지금까지 만든 것

**진입** 과목 선택 → 도트 전환 애니메이션 → 반 선택 → 로그인
**디자인** Padlet 톤(다크 기본), 반·단원마다 다른 9×9 도트 아이콘
**학생** 나의 포트폴리오(차시별 기록) · 홈 · 공지 · 단원(인공지능만) · 과제 제출 · 활동지 · 수업자료
**선생님** 포트폴리오(학생별 + 세특용 텍스트 복사) · 공지 · 수업자료 · 출결 · 학생관리 · 단원 구성 · 활동지
**활동지** 선생님이 직접 만들 수 있음(문항 유형: 글/안내/체크박스/표, 이미지 첨부). 1차시 오리엔테이션(인공지능), 2학기를 시작하며(진로) 들어 있음
**수업자료 슬라이드** PPT를 이미지로 올리면 선생님이 넘기는 대로 학생 화면이 따라옴. 발표 모드(전체화면, ←→·PageUp/Down·B검은화면). 학생은 장마다 메모 가능
**실습 게임** 식물 물 주기(4×4, 30초, 최고점 기록, 5등까지 실명 순위). 슬라이드 중간에 끼워 넣을 수 있음

## 반드시 지켜야 할 것들 (다 대가를 치르고 배운 것)

1. **Firebase 호출은 막히면 실패가 아니라 영원히 대기합니다.** `app.js`의 `withTimeout()`으로 초기 로드에 제한 시간을 걸어뒀습니다. 새 초기 로드를 추가하면 반드시 감싸세요. 안 그러면 화면이 "로딩 중"에서 멈춥니다.
2. **CSS 색 토큰은 `:root`에도 정의하세요.** `[data-theme="dark"]` 안에만 넣으면 라이트 모드에서 값이 비어 요소가 통째로 사라집니다. (informatics와 도메인이 같아 그쪽 테마 설정이 넘어옵니다)
3. **전역 `button{display:inline-flex}`가 있습니다.** 카드형 버튼은 `display:block`을 따로 줘야 내용이 한 줄로 뭉개지지 않습니다.
4. **클릭 핸들러는 `data-action`이 없는 클릭을 걸러냅니다.** 새 버튼에 `id`만 달면 눌러도 아무 일이 안 일어납니다.
5. **CSS는 `css/styles.css` 맨 끝 오버라이드 레이어에만 추가합니다.** 위쪽 원본은 건드리지 않습니다.
6. **작업 후 반드시 배포까지 하고 브라우저로 확인**해주세요. push만 하고 넘기지 마세요.
7. **테스트로 만든 데이터는 반드시 지우세요.** 실제 수업 DB입니다.

## 환경

- Windows. **PowerPoint(COM)와 Python(python-pptx, pywin32, PIL)이 설치돼 있습니다.** pptx 작업은 LibreOffice 말고 PowerPoint COM으로 렌더링해서 확인하세요 (LibreOffice는 이 환경에서 실행 안 됨).
- 로컬 확인은 `python -m http.server` 로 띄우고 브라우저 도구로 검증합니다.

## 남은 일

1. **[중요] informatics 레포 PR #10 머지** — 보안 규칙 정정분입니다. 안 하면 나중에 informatics에서 `firebase deploy` 할 때 이 앱 반이 전부 저장 불가가 됩니다.
2. 진로 6개 반 학생 명단 등록 (지금 test 계정만 있음)
3. 1차시 PPT의 5번 슬라이드 그림이 6요소인데 카드는 4요소 — 옮길지 결정 필요 (`C:\Users\PC\Downloads\인공지능기초_1차시_지능과인공지능.pptx`)

## 말투

한국어로, 결론부터. 확인 없이 단정하지 말고 실제로 돌려보고 알려주세요. 안 되면 안 된다고 솔직히 말해주세요.

---

**이번에 하려는 것:** (여기에 새로 만들 기능을 적으세요)
