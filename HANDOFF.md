# 이어서 작업하기 — 인수인계

새 대화 시작할 때 **아래 `---` 부터 끝까지 그대로 복사해서 붙여넣으면** 됩니다.

---

## ⚠ 먼저 볼 것 — 2026-08-27 부터 바뀜

이제 **`CLAUDE.md` 가 대화마다 자동으로 읽힙니다.** 수업자료를 만들 때 지킬 규칙 26개가
거기 있습니다. 이 파일(HANDOFF)은 **앱 코드와 구조**를 다룹니다.

| 볼 것 | 언제 |
|---|---|
| `CLAUDE.md` | 자동으로 읽힘 — 수업자료 규칙 |
| **`docs/course-map.md`** | **수업자료를 만들기 전 반드시** — 진도·평가·다룰 것/뺄 것 |
| `HANDOFF.md` (이 파일) | 앱 코드·구조·조심할 것 |
| `tools/deck-spec.md` | 슬라이드 배치 |

**반 표기** — `ai-2B`(수·목·금) · `ai-2D`(월·화·금) 로만 부릅니다.
진도표 엑셀의 '2반/3반' 과 어긋납니다 (2반 = ai-2D, 3반 = ai-2B).

## 프로젝트

신동고등학교 **인공지능 기초 · 진로** 수업용 웹앱입니다. 저는 정보 교사이고, 수업 도구를 직접 만들어 씁니다.

| | |
|---|---|
| 레포 | `JunRepos/ai-career` |
| 로컬 | `C:\Users\PC\Desktop\github\ai-career` |
| 배포 | https://junrepos.github.io/ai-career/ (GitHub Pages, main 브랜치) |
| 스택 | 순수 vanilla JS (빌드 없음) + Firebase RTDB/Storage |

`informatics` 레포(정보 교과 앱)에서 갈라져 나왔습니다. **정보 교과 코드(OJ·노트북·미션·퀴즈·AI코딩·기계학습·수행평가)는 하나도 지우지 않고 메뉴에서만 빼둔 상태입니다. 지우지 마세요.**

## 반 구성

| 과목 | 반 id | 인원 |
|---|---|---|
| 인공지능 기초 | `ai-2B`(수3교시) · `ai-2D`(월2교시) | 37 · 16 |
| 진로와 직업 | `career-2-1` ~ `career-2-6` | 20~23명씩 |

- 학생 로그인: **학번 / 학번**, 첫 로그인 시 비밀번호 변경 강제(`isFirstLogin`)
- 확인용 계정: 모든 반에 **`test` / `test`** (의도된 것 — 지우지 마세요)
- 선생님 비밀번호는 informatics 앱과 **같은 것**

## ⚠ Firebase 는 informatics 와 공유합니다

같은 프로젝트(`sindong-informatics`)를 쓰고 데이터는 반 id 로만 갈립니다.
**보안 규칙 파일은 프로젝트 전체를 덮어씁니다.** 규칙을 고칠 일이 생기면 **두 레포의 `database.rules.json`·`storage.rules` 를 같은 내용으로 맞춘 뒤** 배포하세요.

```bash
firebase deploy --only storage --project sindong-informatics
```

- 규칙 반영에 **1분**쯤 걸립니다. 배포 직후 권한 오류가 나면 규칙이 틀린 게 아니니 기다렸다 다시 확인하세요.
- 규칙 비교 전에 **로컬 informatics 가 최신인지 `git fetch` 로 먼저 확인**하세요. (뒤처진 로컬을 보고 "두 레포가 다르다"고 오진한 적 있음)

---

## 지금까지 만든 것

**진입** 과목 선택 → 도트 전환 → 반 선택 → 로그인
**디자인** Padlet 톤(다크 기본), 반·단원마다 다른 9×9 도트 아이콘

### 학생
나의 포트폴리오 · 홈 · 공지 · 단원(인공지능만) · 과제 제출 · 학습지 · 수업자료

### 선생님
포트폴리오(세특용 텍스트 복사) · 공지 · 수업자료 · 출결 · 학생관리 · 단원 구성 · 학습지

### 수업자료 (슬라이드)
- 차시별로 **여러 세트 보관** → 그중 **'이번 시간에 열기'** 한 자료가 학생에게 노출
  - `slides/{반}/decks/{자료id}` · `slides/{반}/live = {on, page, deckId}`
- **여러 반에 한 번에 업로드** (기본: 같은 과목 전체 체크). 여러 반에 올려도 **자료 id 는 하나로 통일**
- **같이 보기** — 선생님이 넘기면 학생 화면이 따라옴
- **발표 모드** (전체화면, ←→·PageUp/Down·B 검은화면·**N 다음 장 미리보기**)
- **🎤 발표자 보기** — 창 두 개. 프로젝터엔 발표 모드, 노트북엔 다음 장 + **내 대본 메모** + 경과 시간
  - 대본 메모는 `slides/{반}/tcNotes/{자료id}/{쪽}` — **학생에게 안 보임** (학생 메모와 별개)
  - ⚠ 브라우저 **팝업 차단 허용** 필요
- 학생은 장마다 메모 가능 (`slides/{반}/notes/{자료id}/{학번}/{쪽}` — 자료별로 분리)
  - 입력칸에 **그림·파일을 끌어다 놓는 것은 막아 뒀습니다** (2026-08-26). 안 막으면 브라우저가 그림 주소를 글자로 넣습니다 — 실제로 한 학생이 13장에 걸쳐 주소만 남긴 적이 있습니다. `js/app.js` 에 문서 전체로 한 번, **발표자 보기 창은 별개 문서라** `js/views/slides.js` 에 따로

### 학습지 (예전 이름 '활동지' — 용어 통일함)
- 기본 학습지는 코드(`js/aiactivity-data.js`)에, **선생님이 만든 것은 반 DB**(`aiactivity/submissions/{반}/customActivities`)에
- 편집기에서 문항 유형 **글 / 안내 / 체크박스 / 표** 를 만들 수 있음
  - 표 **자동 채우기**: 앞선 체크박스에서 고른 보기가 표 첫 칸에 들어감(`fillFrom`)
- 기본 학습지는 **[복제해서 수정]** / **[목록에서 빼기]**(되돌리기 가능), 만든 학습지는 **[수정] [삭제]**
- **[학생에게 보내기]** 를 눌러야 학생 홈에 뜸

### 단원 구성 (인공지능만)
- `수업 자료` / `학습 활동` 두 칸
- 넣을 수 있는 유형: **실습 게임 · 학습지 · 수업자료 · 파일 · 링크 · 글**
- 단원에 건 수업자료는 **수업 때 쓴 필기가 그대로** 남은 채 열림 (혼자 보기 — 선생님을 안 따라감)
- 단원에 건 학습지는 **'학생에게 보내기' 안 눌러도** 열림

### 실습 게임
목록은 `js/games/games.js` 의 **GAMES** 한 곳에서 관리합니다. 게임을 더 만들면
파일 하나 + 여기 한 줄이면 끝입니다. (`views/unit.js` 의 UC_GAMES 가 이걸 그대로 씁니다)

| id | 게임 | 내용 |
|---|---|---|
| `plant-water` | 🌿 식물 물 주기 | 4×4 · 30초 · 끝나면 '방금 쓴 지능' 되짚기 |
| `puzzle-8` | 🧩 8퍼즐 맞추기 | 빈칸과 **같은 줄**의 타일이 밀림 · 적은 수일수록 상위 · 4차시 몰입용 |

- 슬라이드 중간에 끼우기 — 수업자료 조작판의 `＋ 🌿 …` `＋ 🧩 …` 버튼 (지금 장 **다음**에 들어감)
- 단원 구성에도 넣을 수 있음 (유형 '실습 게임' → 드롭다운)
- 점수는 `slides/{반}/scores/{게임id}/{학번}` 에 **최고 기록만**. 저장 함수가 '클수록 좋음' 이라
  적을수록 좋은 게임은 빼서 넣습니다 — 8퍼즐은 `1000 − 수`

> **탐색 단원 도입 활동은 없습니다.** 「숨바꼭질」을 두 판(찾는 쪽 · 숨는 쪽) 만들어 봤지만
> 2026-08-27 에 **둘 다 폐기**했습니다. 코드·CSS·DB 항목을 전부 걷어냈습니다.
> 되살릴 일이 있으면 `.backup/hideseek-2026-08-27/` 에 있습니다.

## 반드시 지켜야 할 것들 (다 대가를 치르고 배운 것)

1. **Firebase 호출은 막히면 실패가 아니라 영원히 대기합니다.** 초기 로드는 `app.js` 의 `withTimeout()`, 업로드는 `firebase.js` 의 멈춤 감시(30초)로 막아뒀습니다. **새 초기 로드·업로드를 추가하면 반드시 같은 방식으로 감싸세요.**
2. **CSS 색 토큰은 `:root` 에도 정의하세요.** `[data-theme="dark"]` 안에만 넣으면 라이트 모드에서 값이 비어 요소가 통째로 사라집니다.
3. **전역 `button{display:inline-flex}` 가 있습니다.** 카드형 버튼은 `display:block` 을 따로 주세요.
4. **클릭 핸들러는 `data-action` 없는 클릭을 걸러냅니다.** `id` 만 달면 눌러도 아무 일이 안 일어납니다.
5. **CSS 는 `css/styles.css` 맨 끝 오버라이드 레이어에만 추가합니다.**
6. **게임·타이머가 도는 화면에서 `render()` 를 반복 호출하지 마세요.** 0.1초마다 다시 그리면 누르는 순간 버튼이 사라져 **클릭이 아예 안 먹습니다.** (물주기가 이 버그였음 — 지금은 `_pwPaint()` 로 값만 갈아끼움)
7. **여러 반을 다루는 기능은 '모든 대상 반'을 처리했는지 확인하세요.** 지금 보고 있는 반만 처리해서 다른 반 자료가 학생 화면에서 조용히 사라진 적 있습니다.
8. **삭제·설정은 반별입니다.** 학습지를 지워도 다른 반엔 그대로 남습니다.
9. **배포 후 `?v=` 를 갱신하세요.** `sh bump-version.sh` 를 돌리면 `index.html` 의 js/css 주소 버전이 바뀌어 학생 브라우저가 새 파일을 받습니다. 안 하면 옛 코드가 계속 돕니다.
10. **작업 후 반드시 배포까지 하고 브라우저로 확인**해주세요. push 만 하고 넘기지 마세요.
11. **테스트로 만든 데이터는 반드시 지우세요.** 실제 수업 DB입니다. (지우기 전 학생 답안이 있는지 먼저 확인)
12. **게임은 `pw*`(물 주기 전용) 함수를 직접 부르지 말고 `games.js` 의 갈래 함수를 쓰세요.**
    `gameView(id)` · `gameLoadRank(id)` · `gameLeaveAll()` · `gameTeacherBoard(id)`.
    `gameDef()` 가 모르는 id 를 물 주기로 떨어뜨리기 때문에, 잘못 불러도 화면이 안 비고
    **엉뚱한 게임이 조용히 뜹니다.** 게임이 물 주기 하나뿐이던 동안은 `UNIT_GAME.gameId`
    (문자열인데 `.gameId` 로 읽어 늘 undefined) 버그가 우연히 맞아떨어져 안 드러났고,
    숨바꼭질을 단원에 걸고서야 발견했습니다. (2026-08-27 고침)

---

## 수업자료(슬라이드)를 새로 만들 때 — 이 흐름이 자리 잡았습니다

**옵시디언에 글로 먼저 쓰고 → 클로드가 pptx 로 → 선생님이 PowerPoint 로 손보고 → PNG → 앱에 올림**

| 어디에 | 무엇이 |
|---|---|
| 차시 노트 | `D:/Google drive/Obsidian Vault/01_Projects/수업/인공지능 기초/` |
| 붙여넣은 그림 | `D:/Google drive/Obsidian Vault/04_Archive/99_media/Pasted image ….png` |
| 새 차시 템플릿 | `D:/Google drive/Obsidian Vault/98_Templates/T_인공지능 차시.md` |
| 교과서 PDF | `D:/Google drive/신동고등학교/01_교과_인공지능/교과서 및 교사용 지도서/고_인공지능 기초(김현철)_교과서.pdf` |
| 덱 원본(JSON) | `tools/samples/lesson4-search.deck.json` 등 |

> 📖 **교과서 쪽 = PDF 쪽 − 3** (PDF 31쪽 = 책 28쪽). `python + fitz` 로 읽습니다.
> 이 교과서에는 **'상태 정보' 라는 말이 없습니다** — 그 자리에 **간선**을 씁니다.

```bash
node tools/deck-build.mjs <덱.json>      # pptx 만들기
node tools/deck-png.mjs <파일.pptx>      # 슬라이드1.PNG … 로 내보내기
node tools/deck-upload.mjs --list        # 반별로 어떤 자료가 있고 무엇이 열려 있는지
node tools/deck-upload.mjs <PNG폴더> --title "5. 지능적 탐색" --classes ai-2B,ai-2D
```

> **`deck-upload.mjs` 는 앱을 안 거치고 바로 올립니다** (Firebase REST). 앱의 '새 자료 올리기'와
> 같은 자리·같은 모양으로 넣고, **두 반에 같은 자료 id** 를 씁니다. 한 반이 실패하면 그 반의
> 조각 파일을 지우고, 끝나면 DB 를 다시 읽어 확인합니다. `--dry` 로 미리 볼 수 있습니다.
> **`이번 시간에 열기`(live)는 건드리지 않습니다** — 수업 때 직접 누르세요.

> ⚠ `deck-png.mjs` 는 **이미 떠 있는 PowerPoint 를 이제 안 닫습니다** (2026-08-27 고침).
> 저장 안 한 편집이 있으면 아예 시작하지 않고 알려 줍니다.

- 디자인(크림 배경·Jua 제목·흰 카드)은 기존 덱에서 뽑아낸 값이라 앞 차시와 이어집니다
- 배치 종류와 옵션은 **`tools/deck-spec.md`** 에 전부 있습니다. 견본은 `tools/samples/layouts-catalog.deck.json`
- 한 장에 안 들어가면 만들 때 ⚠ 경고가 뜹니다

### 슬라이드별로 고칠 것을 적어 두면 그대로 고칩니다 (2026-08-28)

장마다 메모를 적어 두면 클로드가 읽어서 반영합니다. **고쳐 달라는 줄은 맨 앞에 `>>`** 를
붙이세요. 그래야 진짜 대본과 섞이지 않고, `--only` 로 그 줄만 뽑아 옵니다.

```
휴리스틱값 정의는 여기서 천천히.
>> 이 장 제목 너무 기니까 짧게 줄여줘
>> 아래 카드 설명 한 줄 빼도 될 듯
```

적는 곳은 두 군데 중 편한 쪽입니다.

| 어디에 | 언제 편한가 | 읽는 법 |
|---|---|---|
| **앱** — 수업자료에서 장마다 있는 `🎤 N장에서 할 말` 칸 | 이미 올린 자료를 앱·폰으로 넘겨 보며 | `node tools/deck-notes.mjs --id <자료id> --class ai-2B --only` |
| **PowerPoint 발표자 노트** | pptx 를 열어 놓고 손볼 때 | `node tools/deck-notes.mjs --pptx "…/파일.pptx" --only` |

`--only` 를 빼면 메모 전체를 보여줍니다.

**⚠ 고치는 방법이 두 가지고, 어느 쪽이냐에 따라 결과가 다릅니다.**

- **메모만 적으셨으면** — 덱 생성기(`verify/lessonN-deck.py`)를 고쳐 다시 뽑습니다. 깔끔합니다.
- **PowerPoint 로 상자를 옮기는 등 손을 대셨으면** — 다시 뽑으면 그 편집이 날아갑니다.
  그때는 **고칠 장만 따로 만들어** `deck-upload.mjs --replace-page` 로 갈아 끼웁니다.
  어느 쪽인지 먼저 말씀해 주세요.

**앱 메모는 쪽 번호(0부터)로 저장됩니다.** 장을 빼거나 더하면 뒤쪽 메모가 밀립니다.

### 노트에 적는 슬라이드 지시 (선생님과의 약속)
**기본은 지시 없음** — 노트에 수업 내용만 쓰고 배치는 클로드가 정합니다.
원하는 그림이 있을 때만 그 자리에 한 줄:

```markdown
> [!slide] 여기는 표로 비교하면 좋겠어
```

형식 없는 자유 문장입니다. PPT 를 보고 **말로 고쳐도** 됩니다 — 그때는 고쳐 뽑은 뒤
**그 지시를 노트에도 `> [!slide]` 한 줄로 적어 두세요.**

### 수업에서 실제로 갈린 규칙들 (지키세요)
1. **답을 보여주는 장에는 `ask` 를 붙입니다** — 질문이 위에 남고 아래에서 답을 확인합니다. 질문이 사라지면 무엇에 대한 답인지 놓칩니다.
2. **그림을 설명할 때는 `side` 로 그림을 띄워 둡니다** — 트리 용어를 말로만 하면 학생이 대조를 못 합니다.
3. **진행 상황을 알리는 문장 금지** — "여기까지가 오늘의 절반입니다" 같은 것.
4. **선생님에게 하는 말을 슬라이드에 두지 않습니다** — '채점 기준', '발문 —' 은 전부 `notes`(발표자 노트)로. 옅은 상자(`note`)는 **학생에게 하는 말**일 때만.
5. **활동 장에는 시간을 적습니다** — "활동 시작! (15분 동안 진행)".
6. 올릴 때는 **두 반(ai-2B, ai-2D) 모두**, **같은 자료 id** 로.

## 학습지를 새로 만들 때 (이 흐름으로 해주세요)

1. 제가 HTML 이나 초안을 드립니다
2. **`tools/worksheet-preview.mjs` 로 미리보기를 만들어** 아티팩트로 보여주세요
   ```bash
   node tools/worksheet-preview.mjs sheet.json
   ```
   - 앱의 진짜 학습지 CSS 를 긁어 쓰므로 실제 화면과 같습니다
   - **미리보기에서 문장을 직접 고칠 수 있습니다** → 제가 고친 뒤 [고친 내용 복사] 로 돌려드립니다
3. 확정되면 **코드에 넣지 말고 반 DB 에** `saveCustomActivity(반, id, payload)` 로 올려주세요
   - 그래야 제가 앱에서 **수정·삭제**할 수 있습니다 (제목 포함)
   - **두 반(ai-2B, ai-2D) 모두**, **같은 id** 로
   - 기존 학습지를 고치는 거면 **같은 id 로 덮어써서** 중복이 안 생기게
4. **수업자료 슬라이드를 실제로 읽고 학습지와 대조**해 주세요. 용어·단계 수가 어긋나면 학생이 헷갈립니다.

## 종이 학습지(인쇄물)를 만들 때 — 5차시에 자리 잡았습니다

앱 학습지가 아니라 **나눠 주는 종이**입니다. 만드는 길이 다릅니다.

- 원본은 **A4 인쇄용 HTML** 한 파일 (`tools/samples/lesson5-worksheet.html`)
  - `@page { size:A4 portrait; margin:11mm 14mm }` · 본문 폭 `182mm` · 판·트리는 **SVG 로 직접** 그립니다
  - `html,body{background:#fff; color-scheme:light only}` 를 꼭 넣으세요. 없으면 **다크 모드에서 검게** 렌더됩니다
  - 흑백으로 뽑아도 읽히게 — 색은 옅은 회색·검정 위주로
- PDF 는 **헤드리스 크롬**으로 뽑습니다 (LibreOffice 없이 됩니다)
  ```bash
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu \
    --no-pdf-header-footer --print-to-pdf="out.pdf" "file:///…/sheet.html"
  ```
- 뽑은 PDF 는 **눈으로 반드시 확인**하세요. `python + fitz` 로 쪽을 PNG 로 내보내 보면 됩니다
  - 실제로 트리가 아래로 넘쳐 번호 칸이 잘린 적, 제목이 두 줄로 깨진 적이 있습니다
- 슬라이드의 활동 장과 **겹치지 않게** 정리하세요. 종이로 옮겼으면 슬라이드에서는 문제를 빼고
  **'학습지 받고 활동 시작' 한 장 + 답 확인 몇 장**만 남깁니다

---

## 컴퓨터를 갈아엎기 전에 — 백업

```bash
powershell -ExecutionPolicy Bypass -File tools/backup-claude.ps1
```

`D:/Google drive/ai-career-backup/` 에 **깃허브에 없는 것만** 모읍니다. 여러 번 돌려도 됩니다(안 지웁니다).

| 담는 것 | 왜 |
|---|---|
| `~/.claude/projects` 등 | **클로드 코드 대화 기록·메모리.** 레포가 아니라 홈 폴더에 있습니다 |
| `.claude/launch.json` | 미리보기 서버 설정 — `.gitignore` 에 걸려 레포에 없음 |
| 손본 `*.pptx` | `tools/samples/*.pptx` 가 `.gitignore` 라 레포에 없음 |
| `RESTORE.md` | 되살리는 법 |

**되살릴 때 알아야 할 것**

- **레포만 clone 하면 하네스는 다 살아납니다.** `CLAUDE.md`·`docs/`·`tools/`·`verify/` 가
  전부 git 에 있습니다. 새 대화에 `HANDOFF.md` 를 붙여넣으면 맥락도 이어집니다.
  **여기까지가 확실한 부분입니다.**
- **대화 기록 되살리기는 보장이 안 됩니다.** 세션 폴더 이름이 **절대 경로를 인코딩**합니다
  (`C--Users-PC-Desktop-github-ai-career`). 그래서 ① 레포를 **같은 경로**에 두고
  ② `claude-home/` 을 `~/.claude/` 로 되돌린 뒤 ③ `claude --resume` 을 해야 하고,
  사용자 이름이 다르면 폴더 이름을 손으로 바꿔야 합니다.
- 옵시디언 볼트·교과서 PDF 는 원래 구글 드라이브에 있으니 백업 대상이 아닙니다.

⚠ `tools/backup-claude.ps1` 은 **UTF-8 BOM 으로 저장**해야 합니다. PowerShell 5.1 은 BOM 이
없으면 `.ps1` 을 cp949 로 읽어 한글이 전부 깨집니다. (2026-08-28 실제로 겪음)

## 남은 일 / 알아둘 것

### 🔴 보안 — 보류 중 (제가 하자고 하면 그때)
Firebase 규칙에 **인증 개념이 아예 없습니다.** 모든 경로가 `.read: true` + 반 id 정규식만 확인하는 `.write` 입니다.

- `auth/teacher` 가 **읽기·쓰기 모두 열려** 있어 학생이 콘솔에서 **선생님 비밀번호를 덮어쓸 수 있습니다** (전권 탈취)
- `students/{반}` 이 열려 있어 **전교생 학번·이름·비번해시**가 로그인 없이 읽힙니다
- 비번 미변경(=학번) 학생이 반별로 최대 21명

고치려면 로그인 방식 자체를 바꿔야 해서(지금은 클라이언트에서 해시 비교) 설계 결정이 필요합니다.

### 현재 수업 진행 상황 (2026-08-27)

**진도·평가·차시 계획은 전부 `docs/course-map.md` 로 옮겼습니다.** 여기서는 앱 상태만 적습니다.

| | ai-2B | ai-2D |
|---|---|---|
| 수업자료 | 오리엔테이션 17 · 원리 23 · 시스템 19 · 문제 해결과 탐색 37 · 맹목적 탐색 59 · **지능적 탐색 41** | 같음 (문제 해결과 탐색만 36장) |
| 이번 시간 | **맹목적 탐색** (`mtavj6mn4hg31`) | 같음 |
| 학습지 | 나와 인공지능 · 음악 추천 시스템 분석하기 | 같음 |
| 학생 메모 | 77개 | 19개 |

- `3. 문제 해결과 탐색 (이전본 39장)` 은 **2026-08-26 에 두 반 모두 지웠습니다** (그림 78개·메모 13개).
- ⚠ 같은 자료 id 인데 **ai-2B 37장 · ai-2D 36장** 인 자료가 있습니다. 한쪽만 다시 올리면 이렇게 갈립니다.

### 맹목적 탐색 차시 자료 (완료)

- 노트 `5차시. 1-1-2 2) 맹목적 탐색.md` · 덱 원본 `tools/samples/lesson5-blind.deck.json`
- 알고리즘 슬라이드만 따로 `tools/samples/lesson5-algo.deck.json`
- 종이 학습지 `tools/samples/lesson5-worksheet.html` / `.pdf` / `-edit.html`(브라우저에서 글자 고치는 판)
- 올린 것은 선생님이 손본 `인공지능기초_5차시_맹목적탐색3.pptx` 59장

**8퍼즐 판이 두 가지입니다. 헷갈리지 마세요.**

| | 초기 판 | 목표까지 | 쓰는 곳 |
|---|---|---|---|
| 교과서 판 | `2 8 3 / 1 _ 4 / 7 6 5` | 4수 | 교과서·4차시 |
| 학습지 판 | `_ 1 3 / 8 2 4 / 7 6 5` | **2수** | 5차시 학습지·슬라이드 (가~사) |

학습지 판을 따로 만든 이유 — 깊이 2까지만 그리게 하면 교과서 판은 트리 안에 목표가 안 나옵니다.

### 5차시 자료 — 균일 비용 탐색 (완료, 업로드까지)

**2026-08-28 에 계획을 바꿨습니다.** 지능적 탐색(언덕 오르기·최상 우선)을 하려다
**균일 비용 탐색만** 교과서 기반으로 하는 것으로 바꿨습니다. 자세한 건 `docs/course-map.md` 6절.

- 노트 `6차시. 1-1-2 3) 균일 비용 탐색.md` (진도표로는 5차시)
- 덱 생성기 `verify/lesson5-deck.py` · 검산 `verify/lesson5-uniform.py`
- pptx/PNG `tools/samples/인공지능기초_6차시_균일비용탐색.pptx` · `…_png/` **26장**
- 올린 것 — 두 반 `mtc7qv4uq6ddk` 「5. 균일 비용 탐색」

⚠ 앱에 **「5. 지능적 탐색」 40장 자료가 그대로 남아 있습니다.** 언덕 오르기·최상 우선 장이
들어 있어 그대로는 못 씁니다. 6차시 만들 때 손보거나 지우세요.

### 카드 글자 크기 — `def: true` (2026-08-28 추가)

선생님이 "정의 같은 글자는 키워줘" 하셔서 `deck-build.mjs` 의 `cards` 에 `def` 옵션을
넣었습니다. **머리말 36pt · 본문 40.5pt** 로 커집니다. 용어의 정의를 보여주는 장에는 이걸 쓰세요.

### 하네스 구축 — 진행 중 (2026-08-27 시작)

교사가 지적한 세 가지(이상한 부제 · 쓸데없이 늘어난 장수 · 내용 검증 부재)를 막기 위한 것입니다.

| | 무엇 | 상태 |
|---|---|---|
| A | `CLAUDE.md` — 규칙 26개, 대화마다 자동 로드 | **완료** |
| A' | `docs/course-map.md` — 진도·평가 지도 | **완료** |
| B | 내용 검증기 | **차시별로 굴러가기 시작함** — 아래 참고 |
| C | `tools/deck-lint.mjs` — 형식 검사기 | **아직** |
| D | `deck-build.mjs` 디자인 (박스 크면 글자도 크게) | **아직** |

**B 는 5차시(지능적 탐색)에서 실제로 돌려 봤습니다.** 아직 범용 검사기는 없고, 차시마다 이렇게 씁니다.

| 파일 | 하는 일 |
|---|---|
| `verify/lesson6.py` | 알고리즘을 **실제로 돌려** 순서·리스트·횟수를 내놓습니다 |
| `verify/lesson6-deck.py` | 그 결과를 **그대로 읽어** 덱 JSON 을 만듭니다 (손으로 옮겨 적지 않음) |
| `verify/lesson6-layout.mjs` · `-check.mjs` | `diagram` 좌표가 **겹치는지 · 간선이 남의 노드를 스치는지** 검사 |

이렇게 하니 실제로 두 가지를 잡았습니다 — ① 틱택토 휴리스틱값을 가운데 3 으로 잘못 적은 것
(세어 보니 4), ② 간선이 노드를 스쳐 지나가 **없는 길이 있는 것처럼 보이던 그림**.
**덱 JSON 을 손으로 고치지 말고 생성기를 고치세요.** 안 그러면 검산과 어긋납니다.

**B 원래 설계** — 차시마다 `verify/lessonN.py` 를 쓰고, 그 안에서 실제로 계산한 값을 `facts` 로 내놓습니다.
검사기는 ① 덱에 쓴 값이 `facts` 와 다르면 중단 ② **덱에 나오는 숫자·순서 중 `facts` 에 없는 것을
"검산 안 된 값" 으로 전부 목록 출력**합니다. 알고리즘 전용이 아니라 어느 차시에나 씁니다.

개념(계산이 아닌 것)은 **출처 추적**으로 잡습니다. 정의문마다 출처(교과서 쪽 / 노트 / 보충)를 달고,
교과서 출처면 그 쪽 원문에 핵심 어구가 실제로 있는지 대조합니다. **출처 없는 문장 = 지어낸 것**입니다.
### 환경
- Windows. **PowerPoint(COM)·Python(python-pptx, pywin32, PIL)·Node** 설치돼 있음. 슬라이드 생성기는 `pptxgenjs`(`npm install`) 를 씁니다. pptx 작업은 LibreOffice 말고 PowerPoint COM 으로 렌더링해 확인 (LibreOffice 는 이 환경에서 실행 안 됨)
- 로컬 확인은 `.claude/launch.json` 의 `ai-career` (http://localhost:5599)

---

## 말투

한국어로, **결론부터**. 확인 없이 단정하지 말고 **실제로 돌려보고** 알려주세요. 안 되면 안 된다고 솔직히 말해주세요. 제가 모르는 걸 임의로 정하지 말고 **모르면 물어보세요.**

---

**이번에 하려는 것:** `docs/course-map.md` 의 「6차시 — A\* · 게임 트리」 항목을 보세요.
**ai-2B 는 09-03 · ai-2D 는 09-01** 입니다.

5차시(지능적 탐색) 자료는 **두 반에 올렸습니다** (40장 · 자료 id `mtb9mukkdas19`).
다만 **`이번 시간에 열기` 는 안 눌렀습니다** — 수업 시작할 때 앱에서 누르세요.

