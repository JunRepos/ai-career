# 이 레포에서 지킬 것

신동고 **인공지능 기초 · 진로** 수업앱입니다. 사용자는 정보 교사이고, 여기서 만드는 것은
**실제 수업에 그날 쓰는 자료**입니다. 잘못된 내용이 그대로 학생에게 나갑니다.

| 무엇을 볼 때 | 어디 |
|---|---|
| **이번 차시가 어디쯤이고 무엇을 뺐는지** | **`docs/course-map.md`** ← 자료 만들기 전 반드시 |
| 프로젝트 구조·반 구성·앱 코드에서 조심할 것 | `HANDOFF.md` |
| 슬라이드 배치 종류와 옵션 | `tools/deck-spec.md` |

이 파일은 **수업자료를 만들 때 지킬 규칙**만 담습니다.

**반은 `ai-2B`(수·목·금) · `ai-2D`(월·화·금) 로만 부릅니다.**
진도표 엑셀의 '2반/3반' 과 어긋나므로 (2반 = ai-2D, 3반 = ai-2B) 그 표기를 쓰지 않습니다.

---

## 0. 자주 쓰는 자리

| 무엇 | 어디 |
|---|---|
| 교과서 PDF | `D:/Google drive/신동고등학교/01_교과_인공지능/교과서 및 교사용 지도서/고_인공지능 기초(김현철)_교과서.pdf` |
| 차시 노트 | `D:/Google drive/Obsidian Vault/01_Projects/수업/인공지능 기초/` |
| 노트에 붙인 그림 | `D:/Google drive/Obsidian Vault/04_Archive/99_media/` |
| 덱 원본(JSON) | `tools/samples/lessonN-*.deck.json` |
| 배치 설명서 | `tools/deck-spec.md` |

> **책 쪽 = PDF 쪽 − 3** (PDF 33쪽 = 책 30쪽)
> 교과서는 `python + fitz` 로 읽고 **결과를 UTF-8 파일로 저장한 뒤 읽습니다.**
> 콘솔로 바로 출력하면 한글이 깨집니다(cp949).

```bash
node tools/deck-build.mjs <덱.json>      # pptx 만들기
node tools/deck-png.mjs <파일.pptx>      # 슬라이드1.PNG … 로 내보내기
```

---

## 1. 시작 전 — 매번

0. **`docs/course-map.md` 를 먼저 읽는다.** 이번 차시가 어디쯤인지, 다루기로 한 것과
   빼기로 한 것이 무엇인지 확인한다. 자료를 만든 뒤에는 **진도표 상태를 갱신한다.**
1. 수업자료를 만들기 전 **교과서 해당 쪽을 먼저 읽는다.** 읽지 않고 시작하지 않는다.
2. 옵시디언 **차시 노트를 처음부터 끝까지** 읽는다. 교과서와 노트가 다르면 **노트가 우선**이다.
3. 노트에 `> [!slide]` 지시가 있으면 그대로 따른다. 지시가 없으면 배치는 내가 정한다.

## 2. 슬라이드 — 넣지 않는 것

4. **부제를 넣지 않는다.** 표지 `subtitle`, 제목 옆 작은 글씨, `Breadth-First Search` 같은
   영문 병기, 한 줄 감상형 `desc` — 전부 뺀다. **정보가 아니면 넣지 않는다.**
5. **군더더기 문장을 넣지 않는다.** "한마디로 …", "말하자면 …" 같은 말바꿈,
   억지 비유는 넣지 않는다.
   **단, 설명 자체는 자세히 쓴다.** (2026-09-02 지시) 처음 배우는 학생이 읽고 알아들을 수 있어야 한다.
   - 정의만 던지지 말고 **값으로 확인하는 장**을 뒤에 붙인다 (예: 누적 비용 정의 → a→c→d 는 4+3=7)
   - 여러 단계로 진행되는 것은 **한 단계에 한 장씩** 쪼갠다 (A* 진행을 두 장에 몰아넣지 않는다)
   - 활동을 시키기 전에 **규칙·계산법을 하나씩** 짚는다
   - 장수는 넉넉해도 된다. 한 장에 욱여넣는 것보다 나눈다.
6. **감성적·문학적 제목 금지.** 제목은 내용 그대로 쓴다.
   `어느 쪽부터 펼칠 것인가` → `맹목적 탐색` · `트리를 층으로 훑는다` → `너비 우선 탐색`
7. 진행 상황을 알리는 문장 금지. ("여기까지가 오늘의 절반입니다")
8. 교사에게 하는 말은 슬라이드에 두지 않는다. 전부 `notes`(발표자 노트)로.

## 3. 슬라이드 — 배치

9. 용어 설명은 그림·도식과 **합칠 수 있으면 합치고, 넘치면 나눈다.**
   도식(트리·그래프)은 **먼저 한 장에 크게** 보여주고, 다음 장에서 **축소해 왼쪽에 두고
   설명을 오른쪽에** 붙인다(`side`). 이 방식을 기본으로 한다.
10. **박스가 크면 글자도 크게.** 큰 박스에 작은 글씨를 넣지 않는다. 한눈에 읽히는 것이 기준.
11. 한 장에 한 가지만. 대신 그 한 가지는 **그림과 용어가 함께** 있어야 한다.
12. 답을 보여주는 장에는 `ask` 로 질문을 남긴다. **카드·표처럼 글로 된 답 장에만 남기고,
    큰 그림(트리·판 여러 개) 장에는 남기지 않는다** — 그림이 눌려 겹친다.
13. 활동을 시키는 장에는 시간을 적는다. ("15분 동안 진행")

## 4. 내용 검증 — 어긴 적 있음

14. **절차·알고리즘·순서는 반드시 코드로 실행해 검산한 뒤에만** 슬라이드에 넣는다.
    적어 놓은 단계대로 돌렸을 때 슬라이드에 쓴 순서가 나오는지 확인한다.
    **교과서 문장을 눈으로 옮겨 적기만 하는 것 금지.** (BFS 알고리즘을 이렇게 틀린 적 있음)
15. 숫자(개수·깊이·횟수)는 **직접 세어 본 값**만 쓴다.
16. 교과서에 없는 내용을 넣을 때는 **채팅으로 알린다.**
    슬라이드·학습지에는 "교과서 밖" 같은 표시를 **넣지 않는다.**
17. 교과서와 노트가 어긋나면 **채팅으로 어느 쪽을 따랐는지 보고한다.**
    자료에는 결론만 담고, 교과서와 다르다는 언급을 **넣지 않는다.**
17-1. **탐색 단원은 12월 정기시험 범위다.** 용어와 정의가 흔들리면 안 된다.
    `docs/course-map.md` 의 성취기준·평가 요소와 어긋나지 않는지 확인한다.

## 5. PPT 와 학습지

18. **PPT 와 학습지의 용어·정의·예시 판은 글자 그대로 같아야 한다.**
    한쪽을 고치면 다른 쪽도 같이 고친다.
19. 같은 대상을 두 자료에서 다른 판·다른 이름으로 쓰지 않는다.
    불가피하면 **왜 다른지 노트에 적는다.**
20. 학습지를 고친 뒤에는 **PDF 를 PNG 로 렌더해 눈으로 확인**한다.
    넘침·잘림·줄바꿈 깨짐을 본 뒤에 전달한다.

## 6. 교사가 손본 것 보호

21. **교사가 손본 파일을 덮어쓰지 않는다.** PPT 를 이미 편집했으면 전체를 다시 뽑지 말고
    **고칠 슬라이드만 따로 만들어** 전달한다.
21-1. **슬라이드별 수정 요청은 장마다 적힌 메모로 온다.** 자료를 고치기 전에 먼저 읽는다.
    `node tools/deck-notes.mjs --id <자료id> --class ai-2B --only` (앱의 '🎤 N장에서 할 말')
    `node tools/deck-notes.mjs --pptx "<파일.pptx>" --only` (PowerPoint 발표자 노트)
    `>>` 로 시작하는 줄이 고쳐 달라는 것이다. **메모만 적혔으면 생성기를 고쳐 다시 뽑고,
    PowerPoint 로 손댄 것이 있으면 고칠 장만 만들어 `--replace-page` 로 갈아 끼운다.**
22. **PowerPoint 에 저장 안 한 편집이 있으면 `deck-png.mjs` 를 돌리지 않는다.**
    (2026-08-27 고침) 이제 이미 떠 있는 PowerPoint 는 닫지 않고, 저장 안 한 편집이 있으면
    스스로 멈추고 알려 준다. 그래도 **돌리기 전에 열려 있는지 확인**하고, 저장 안 한 것이
    있으면 저장해 달라고 요청한다.

## 7. 올리고 지우기

23. 올릴 때는 **두 반(ai-2B, ai-2D) 모두, 같은 자료 id** 로.
    `node tools/deck-upload.mjs <PNG폴더> --title "…" --classes ai-2B,ai-2D` 로 올린다.
    **`이번 시간에 열기`(live)는 건드리지 않는다** — 수업 때 교사가 직접 누른다.
24. 지우기·배포처럼 되돌릴 수 없는 것은 **먼저 확인받는다.** 지우기 전 백업 파일을 전달한다.
25. 작업 뒤 **DB 를 실제로 읽어** 반영됐는지 확인하고 보고한다.

## 8. 막혔을 때

26. **막히거나 선택지가 갈리면 추정하지 말고 묻는다.** 모르는 것을 그럴듯하게 채워 넣지 않는다.
    파일을 못 찾거나, 두 방법 중 골라야 하거나, 노트에 없는 내용을 지어내야 하면 **먼저 물어본다.**
